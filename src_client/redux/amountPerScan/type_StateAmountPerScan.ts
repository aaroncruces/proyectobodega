/**
 * Sencillamente un type para enforzar la estructura del state en los elementos
 * que usan esta propiedad del producto.
 */
type StateAmountPerScan = {
  amountPerScan: number;
};
export default StateAmountPerScan;
