/**
 * identificadores para que el reducer (marcaReducer)
 * sepa que la accion es la de actualizar el marca o la de borrar el texto
 */
enum MarcaActionsTypes {
  SET_MARCA = "SET_MARCA",
  RESET_MARCA = "RESET_MARCA",
}
export default MarcaActionsTypes;
